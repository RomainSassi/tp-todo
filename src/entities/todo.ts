export type Todo = {
    id: string,
    title: string,
    content: string,
    completed: boolean,
    createdAt: string,
    endDateTime: string,
    marked: boolean,
    location: string,
    category: number
}